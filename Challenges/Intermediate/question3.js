// TODO: Finish the two forEach loops so that one returns the values of the array (a, b, c) and the other returns the indexes of the array (0, 1, 2)

["a", "b", "c"].forEach();

["a", "b", "c"].forEach();
